The database is hosted in mysql 5.1 or above.

the database is named ccvote on the web host.

1. CCC_vote.sql is a script to create the schema. Use this command "mysql ccvote < CCC_vote.sql" to populate the schema. It wlll create the schema if needed and the user has appropriate permissions.

2. The MySQL Workbench model is stored in ccvote.mwb.

3. The schema diagram is stored in ccvote.pdf


